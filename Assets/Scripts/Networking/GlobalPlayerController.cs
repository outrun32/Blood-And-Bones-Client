using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalPlayerController : MonoBehaviour
{
    /*public PlayerManager playerManager;
    public CharacterController characterController;

    private void Awake()
    {
        moveCharacterController = new MoveCharacterController(characterController, this.transform);
        playerManager.characterController = moveCharacterController;
    }

    private void FixedUpdate()
    {
        moveCharacterController.FixedUpdate();
    }*/
}
